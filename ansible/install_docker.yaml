- name: Install docker
  become: yes
  hosts: localhost
  tasks:
    - name: Install docker.io
      apt:
        name: docker.io
        update-cache: yes
        state: present
    - name: print message
      debug:
        msg: "Docker installed successfully."
